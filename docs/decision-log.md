# Decision Log

## ADR-001

- 결정: core 모듈은 Spring 의존성 없이 유지한다.
- 이유: 라이브러리 사용 범위를 넓히고 테스트를 단순하게 유지하기 위해서다.

## ADR-002

- 결정: Spring 연동은 별도 모듈로 분리한다.
- 이유: 순수 Java 사용 방식과 Spring 사용 방식을 분리해 API 표면을 명확하게 유지하기 위해서다.
